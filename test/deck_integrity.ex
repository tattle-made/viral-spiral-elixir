defmodule Viralspiral.DeckIntegrity do
  use ExUnit

  test "all topical and affinity cards have a non empty fakes array" do
  end

  test "bias cards have a fake field" do
  end

  test "all red, blue, topical, cat, sock cards have images" do
  end
end
